#!/usr/bin/env bash

. hooks/molecule.rc

mkdir \
  --parents \
  "${COLLECTION_DIR}"

rsync \
  --archive \
  --verbose \
  --recursive \
  --exclude hooks \
  --exclude __pycache__ \
  --delete \
  ../$COLLECTION_NAME/* \
  "${COLLECTION_DIR}/"
